# .vercelignore

# Exclude Git directory to prevent uploading repository history
.git/

# Exclude Python cache files and virtual environments
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
env/
ENV/

# Exclude environment variable files (ensure sensitive data isn't exposed)
.env
.env.local
.env.*.local

# Exclude Node.js dependencies if present (optional, since it's a Django project)
node_modules/

staticfiles/
build/
dist/
out/

# Exclude log files
*.log

# Exclude temporary files and OS-specific files
.DS_Store
Thumbs.db
*.swp

# Exclude media files if they are large and not required for deployment
# You can host these externally (e.g., AWS S3, Google Cloud Storage) for better performance
ForecastApp/static/ForecastApp/data/images/*.mp4

# Optionally, exclude specific large files if not needed
# If certain images or media are necessary, remove them from this list
